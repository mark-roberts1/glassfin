name: build-appimage

on:
  workflow_call:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - name: Build AppImage
      env:
        WORKSPACE: ${{ github.workspace }}
      run: |
        docker build \
          -t glassfin-appimage \
          -f dev/appimage/Dockerfile \
          .
        mkdir -p output
        docker run --rm \
          -v "${WORKSPACE}/output:/host-output" \
          glassfin-appimage
    - name: Archive production artifacts
      uses: actions/upload-artifact@v7
      with:
        name: linux-appimage-x86_64
        path: ${{ github.workspace }}/output/Glassfin-*.AppImage
